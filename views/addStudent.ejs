<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Student</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    
    <style>
        body {
            background: #0f172a; /* Dark futuristic background */
            color: #e2e8f0;
        }
        .card {
            background: rgba(15, 23, 42, 0.9); /* Translucent card */
            border: 2px solid #3b82f6;
            border-radius: 16px;
            box-shadow: 0 0 15px #3b82f6;
        }
        .form-control {
            background-color: transparent;
            border: 2px solid #3b82f6;
            color: #e2e8f0;
        }
        .form-control::placeholder {
            color: #94a3b8;
        }
        .btn-primary {
            background: #3b82f6;
            border: none;
            transition: transform 0.3s ease;
        }
        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 0 15px #3b82f6;
        }
        .btn-outline-secondary {
            color: #94a3b8;
            border-color: #94a3b8;
            transition: transform 0.3s ease;
        }
        .btn-outline-secondary:hover {
            transform: translateY(-3px);
            background: #64748b;
            color: #e2e8f0;
        }
        .form-label{
            color:white;
        }
    </style>
</head>

<body>

<div class="container my-5">
    <h2 class="text-center text-info mb-4">Add New Student</h2>

    <div class="card shadow-sm p-4">
        <div class="card-body">
            <form action="/students/add" method="POST">
                <div class="mb-3">
                    <label for="name" class="form-label">Name</label>
                    <input type="text" class="form-control" id="name" name="name" placeholder="Enter student name" required>
                </div>
                <div class="mb-3">
                    <label for="age" class="form-label">Age</label>
                    <input type="number" class="form-control" id="age" name="age" placeholder="Enter student age" required>
                </div>
                <div class="mb-3">
                    <label for="contact" class="form-label">Contact</label>
                    <input type="text" class="form-control" id="contact" name="contact" placeholder="Enter contact number" required>
                </div>
                <div class="mb-3">
                    <label for="enrollmentYear" class="form-label">Enrollment Year</label>
                    <input type="number" class="form-control" id="enrollmentYear" name="enrollmentYear" placeholder="Enter enrollment year" required>
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn btn-primary">Add Student</button>
                </div>
            </form>
        </div>
    </div>

    <div class="text-center mt-4">
        <a href="/students" class="btn btn-outline-secondary">Back to Student List</a>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
